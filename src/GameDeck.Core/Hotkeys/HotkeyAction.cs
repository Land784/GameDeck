namespace GameDeck.Core.Hotkeys;

/// <summary>Values are stable RegisterHotKey ids — never renumber.</summary>
public enum HotkeyAction
{
    PlayPause = 1,
    NextTrack = 2,
    PreviousTrack = 3,
    ToggleOverlay = 4,
    ToggleInteractivity = 5,
    SkipAd = 6,
}
